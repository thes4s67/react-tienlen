import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#11192a",
    },
    secondary: {
      main: "#97BC62",
    },
    info: {
      main: "#fff",
    },
  },
});

export default theme;
